`script1.m` : script de demo de notre code

`SV_blur.m` : notre implementation d'une d'accentuation de flou en fonction d'une carte de defocus

`segment1.m` : notre fonction de segmentation

`grad.m` : notre fonction pour calculer le gradient

`getLaplacian1.m` : fonction qui donne la matrice Matting Laplacian

`bfilter.m` : fonction qui implemente le Joint Bilateral Filter

dossier `leur_code` : les codes des auteurs de l'article

`leur_code/demo.m` : demonstration de leur code, nous y avons aussi ajouté notre accentuation de flou
